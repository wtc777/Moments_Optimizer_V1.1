spring:
  application:
    name: moments-optimizer-backend
  profiles:
    active: dev
  config:
    import: optional:classpath:application-local.yml

server:
  port: 8080

moments:
  worker:
    enabled: true
  model:
    client-type: stub
    base-url: ""
    auth-header: ""
    auth-token: ""
    timeout-ms: 5000
